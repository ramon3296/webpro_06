<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>曜日計算</title>
</head>
<body>
  <h1>曜日計算</h1>
  <form action="/weekday" method="get">
    <label for="date">日付を入力 (YYYY-MM-DD):</label>
    <input type="date" id="date" name="date" required>
    <button type="submit">曜日を計算</button>
  </form>
  <% if (date && day) { %>
    <p>入力された日付: <%= date %></p>
    <p>その日の曜日: <%= day %></p>
  <% } %>
</body>
</html>
